# v1.1.x

## v1.1.0

### 传感器动态注册

- **图表动态显示/隐藏**：传感器数据图表支持动态开关，用户可根据需要只显示关键传感器曲线，聚焦于重要数据。
- **可归零属性（zeroable）**：传感器配置文件新增 `zeroable` 属性，仅对可归零的传感器显示归零按钮，精简界面。
- **动态数据录制**：数据录制与传感器注册机制联动，自动记录已激活的传感器数据。
