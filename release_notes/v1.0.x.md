# v1.0.x

# v1.0.0

- use new data key for data from hepic_server, since this makes HEPiC incompatible with older versions (<1.0.0) hepic_server, we bump to HEPiC v1.0.0 to highlight this change. 
- there are more planned changes: e.g. communicate config file, plots that can be toggled on / off, that will complete version 1.0.0. As for now, we develop a minimal working version as the start of this new version to keep the system online