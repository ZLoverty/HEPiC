# v1.0.x

## v1.0.0

> **注意**：v1.0.0 与旧版 hepic_server（< 1.0.0）不兼容，需同步升级 hepic_server。

### 数据接口

- **更新 hepic_server 数据键名**：适配 hepic_server 新版数据格式，修改客户端解析所用的字段名称。此变更与旧版 hepic_server 不兼容，版本号因此升至 1.0.0 以明确标识兼容性边界。

### 修复

- **速度目标值初始化**：修复目标进给速度（target feedrate）在特定情况下未能正确刷新为 0 的问题。
